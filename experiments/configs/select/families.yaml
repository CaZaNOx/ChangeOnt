# Which problem variants to run per family, with their parameters

bandit:
  easy:   { probs: [0.1, 0.2, 0.8], horizon: 2000, seeds: [1,2,3] }
  medium: { probs: [0.3, 0.5, 0.6], horizon: 4000, seeds: [1,2,3] }
  # hard:  { probs: [0.49, 0.5, 0.51], horizon: 6000, seeds: [1,2,3] }

maze:
  default5: { spec: { width: 5, height: 5, seed: 0 }, episodes: 5 }
  # default9: { spec: { width: 9, height: 9, seed: 0 }, episodes: 5 }

renewal:
  clean:
    env:   { A: 8, L_win: 6, p_ren: 0.02, p_noise: 0.0, T_max: 1000 }
    steps: 1000
    seeds: [7,8,9]
