# H_SSI

Status: canonical active header.

`H_SSI` carries six-question shape/direct-control state into the kernel without opening a non-CO route. It supports gauge, retention, burden, quotient, recursion, and collapse tolerances used by the active execution loop.
