# PATH: experiments/configs/cli_renewal.yaml
seed: 7
steps: 1000
mode: phase          # CLI will override this with --agents (last|phase|ngram|haq)
out_dir: outputs/renewal_cli
env:
  A: 8
  L_win: 6
  p_ren: 0.02
  p_noise: 0.00
  T_max: 1000
