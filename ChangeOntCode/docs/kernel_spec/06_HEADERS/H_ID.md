# H_ID

Status: removed from the certified CO runtime.

The canonical implementation target uses `H_SSI` with the six-question public shape path. `H_ID` is not an active evidence-bearing header and must not be selected by canonical configs.
