# CO combos — canonical set

Only `CO_canonical_core.yaml` is part of the evidence-bearing CO runtime.

The active docs define the runtime loop as:

```text
Boundary / Adapter
→ CandidateSurface
→ Continuation Identity
→ Burden Operations
→ RelationSurface
→ RecursiveContinuationField
→ CollapseCertificate
→ CommitmentSurface
```

The canonical combo surface contains only evidence-bearing CO runtime configuration.
External baselines live in experiments/studies and must remain comparison-only.
