agents:
  - name: co_EA_h4_a02
    enabled: true
    family: co
    params:
      meta_header:
        prior_classicality: 0.5
        rule_stability: 0.8
        expected_dynamism: 0.4

      header:
        mode: SSI

      primitives:
        P2:
          enabled: true
        P7:
          enabled: true

      elements:
        EA_HAQ:
          enabled: true
          history_len: 4
          ema_alpha: 0.2
          weight: 1.0

      groups:
        - name: g_ea
          members: [EA_HAQ]
          operator: add
          weight: 1.0

      final_fusion:
        operator: weighted_select
        classical_weight: 0.0
        co_weight: 1.0

  - name: co_EA_h8_a02
    enabled: true
    family: co
    params:
      meta_header:
        prior_classicality: 0.5
        rule_stability: 0.8
        expected_dynamism: 0.4

      header:
        mode: SSI

      primitives:
        P2:
          enabled: true
        P7:
          enabled: true

      elements:
        EA_HAQ:
          enabled: true
          history_len: 8
          ema_alpha: 0.2
          weight: 1.0

      groups:
        - name: g_ea
          members: [EA_HAQ]
          operator: add
          weight: 1.0

      final_fusion:
        operator: weighted_select
        classical_weight: 0.0
        co_weight: 1.0

  - name: co_EA_h8_a05
    enabled: true
    family: co
    params:
      meta_header:
        prior_classicality: 0.5
        rule_stability: 0.8
        expected_dynamism: 0.4

      header:
        mode: SSI

      primitives:
        P2:
          enabled: true
        P7:
          enabled: true

      elements:
        EA_HAQ:
          enabled: true
          history_len: 8
          ema_alpha: 0.5
          weight: 1.0

      groups:
        - name: g_ea
          members: [EA_HAQ]
          operator: add
          weight: 1.0

      final_fusion:
        operator: weighted_select
        classical_weight: 0.0
        co_weight: 1.0