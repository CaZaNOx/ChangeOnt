# Header Spec Requirements

Status: canonical header target.

The certified runtime uses `H_SSI`. Header state may modulate gauge/tolerance/direct controls, but must not route to non-CO policy, first-legal selection, score-only rescue, or any external baseline.
