# Canonical default path keeps investigatory candidates off unless explicitly enabled
#ChangeOntCode\experiments\configs\co_agents\co_agents_selection.yaml
apply_to_all_families: true
apply_to_all_modes: true

co_agents:
  # ===== Full CO blend (unchanged behavior, just explicit classic_planner) =====
  - type: "co"
    name: "CO_full"
    params:
      header: { mode: "SSI" }

      elements:
        haq:         { enabled: true,  history_len: 64, ema_alpha: 0.2 }
        ghvc:        { enabled: false }
        EC_Identity: { enabled: true }
        density:     { enabled: true,  rounding: 2 }
        change_ops:  { enabled: true,  k: 4, mdl_select: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "co_only"          # canonical CO-only deployment
          classic_planner: "greedy"
          use_classical_proposal: false
          allow_classical_fallback: false
          allow_policy_rescue: false
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10

      primitives:
        signal_bus: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}

      combinator:
        order: ["haq","ghvc","EC_Identity","density","change_ops","candidate_surface","commitment_surface"]

  # ===== QA-only: same as CO_full, but with aggressive birth thresholds =====
  - type: "co"
    name: "CO_full_QA"
    params:
      header: { mode: "SSI" }

      elements:
        haq:         { enabled: true,  history_len: 64, ema_alpha: 0.2 }
        ghvc:        { enabled: true,  mdl_lambda: 1.0, cooldown: 1, birth_threshold: 0.01 }
        EC_Identity: { enabled: true }
        density:     { enabled: true,  rounding: 2 }
        change_ops:  { enabled: true,  k: 4, mdl_select: true }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "co_only"
          classic_planner: "greedy"
          use_classical_proposal: false
          allow_classical_fallback: false
          allow_policy_rescue: false
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10

      primitives:
        signal_bus: {}
        P3: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}

      combinator:
        order: ["haq","ghvc","EC_Identity","density","change_ops","candidate_surface","commitment_surface"]

  # ===== Router: classical-only with A* + translator mask guardrails =====
  - type: "co"
    name: "CO_router"
    params:
      header: { mode: "SSI" }

      elements:
        # You can disable HAQ too; left on here since it's cheap and may enrich future routing logic
        haq:         { enabled: true,  history_len: 64, ema_alpha: 0.2 }
        ghvc:        { enabled: false }
        density:     { enabled: false }
        change_ops:  { enabled: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true     # harmless when classic_only; bus still logged
          use_translator: true            # keeps anti-oscillation / blocking masks
          blend_mode: "classic_only"      # <- FORCE classical path
          classic_planner: "astar"        # <- use A* as the classical planner
          co_weight_override: 0.0         # <- belt & suspenders: ensures CO weight is zero
          eps_on_cycle: 0.00
          ngram_order: 0
          greedy_explore_bias: 0.00

      primitives:
        signal_bus: {}
        P0: {}
        P7: {}
        bandit_stats: {}

      combinator:
        order: ["haq","candidate_surface","commitment_surface"]

  # ===== Experiment doctrine: element isolation =====
  - type: "co"
    name: "CO_EA_only"
    params:
      header: { mode: "SSI" }
      elements:
        haq:         { enabled: true,  history_len: 64, ema_alpha: 0.2 }
        ghvc:        { enabled: false }
        EC_Identity: { enabled: false }
        density:     { enabled: false }
        change_ops:  { enabled: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "mix"
          classic_planner: "greedy"
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10
      primitives:
        signal_bus: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}
      combinator:
        order: ["haq","candidate_surface","commitment_surface"]

  - type: "co"
    name: "CO_EB_only"
    params:
      header: { mode: "SSI" }
      elements:
        haq:         { enabled: false }
        ghvc:        { enabled: true,  mdl_lambda: 1.0, cooldown: 5, birth_threshold: 2.0 }
        EC_Identity: { enabled: false }
        density:     { enabled: false }
        change_ops:  { enabled: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "mix"
          classic_planner: "greedy"
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10
      primitives:
        signal_bus: {}
        P3: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}
      combinator:
        order: ["ghvc","candidate_surface","commitment_surface"]

  - type: "co"
    name: "CO_EC_only"
    params:
      header: { mode: "SSI" }
      elements:
        haq:         { enabled: false }
        ghvc:        { enabled: false }
        EC_Identity: { enabled: true }
        density:     { enabled: false }
        change_ops:  { enabled: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "mix"
          classic_planner: "greedy"
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10
      primitives:
        signal_bus: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}
      combinator:
        order: ["EC_Identity","candidate_surface","commitment_surface"]

  # ===== Experiment doctrine: element combination =====
  - type: "co"
    name: "CO_EA_EB"
    params:
      header: { mode: "SSI" }
      elements:
        haq:         { enabled: true,  history_len: 64, ema_alpha: 0.2 }
        ghvc:        { enabled: true,  mdl_lambda: 1.0, cooldown: 5, birth_threshold: 2.0 }
        EC_Identity: { enabled: false }
        density:     { enabled: false }
        change_ops:  { enabled: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "mix"
          classic_planner: "greedy"
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10
      primitives:
        signal_bus: {}
        P3: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}
      combinator:
        order: ["haq","ghvc","candidate_surface","commitment_surface"]

  # ===== Experiment doctrine: header/meta-header variation =====
  - type: "co"
    name: "CO_header_CS"
    params:
      header: { mode: "CS" }
      elements:
        haq:         { enabled: true,  history_len: 64, ema_alpha: 0.2 }
        ghvc:        { enabled: false }
        EC_Identity: { enabled: true }
        density:     { enabled: true,  rounding: 2 }
        change_ops:  { enabled: true,  k: 4, mdl_select: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "mix"
          classic_planner: "greedy"
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10
      primitives:
        signal_bus: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}
      combinator:
        order: ["haq","ghvc","EC_Identity","density","change_ops","candidate_surface","commitment_surface"]

  - type: "co"
    name: "CO_meta_prior"
    params:
      meta_header:
        priors:
          stability_prior: 0.6
          change_prior: 0.4
      header: { mode: "SSI" }
      elements:
        haq:         { enabled: true,  history_len: 64, ema_alpha: 0.2 }
        ghvc:        { enabled: false }
        EC_Identity: { enabled: true }
        density:     { enabled: true,  rounding: 2 }
        change_ops:  { enabled: true,  k: 4, mdl_select: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "mix"
          classic_planner: "greedy"
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10
      primitives:
        signal_bus: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}
      combinator:
        order: ["haq","ghvc","EC_Identity","density","change_ops","candidate_surface","commitment_surface"]

  # ===== Experiment doctrine: semantic combinator swap (global) =====
  - type: "co"
    name: "CO_semantic_swap"
    params:
      semantic_overrides:
        SC_AdditiveBlend: "SC_MultiplicativeCoupling"
      header: { mode: "SSI" }
      elements:
        haq:         { enabled: true,  history_len: 64, ema_alpha: 0.2 }
        ghvc:        { enabled: false }
        EC_Identity: { enabled: true }
        density:     { enabled: true,  rounding: 2 }
        change_ops:  { enabled: true,  k: 4, mdl_select: false }
        candidate_surface: { enabled: true }
        commitment_surface:
          enabled: true
          prefer_bus_if_present: true
          use_translator: true
          blend_mode: "mix"
          classic_planner: "greedy"
          co_weight_override: null
          eps_on_cycle: 0.10
          ngram_order: 2
          greedy_explore_bias: 0.10
      primitives:
        signal_bus: {}
        P0: {}
        P7: {}
        bandit_stats: {}
        ngram_model: {}
      combinator:
        order: ["haq","ghvc","EC_Identity","density","change_ops","candidate_surface","commitment_surface"]
