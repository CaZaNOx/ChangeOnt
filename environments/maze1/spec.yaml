# Maze1 environment specification (freeze after validation)
name: maze1
params:
  width: 5
  height: 5
  seed: 0
allowed_baselines:
  - agents.stoa.agent_fsm
  - agents.stoa.agent_lstm
  - agents.stoa.agent_transformer_lite