# Structural Baseline Freeze — 2026-05-16

Status: **candidate structural baseline for controlled validation**.

This freeze is not a proof of ChangeOnt, RCF novelty, reward performance, or consciousness. It records the current architecture and constants so later tests have a stable object.

## Frozen current loop

```text
Boundary / Adapter
→ CandidateSurface
→ Continuation identity / branch-internal burden operations
→ RelationSurface
→ RecursiveContinuationField
→ CollapseCertificate
→ CommitmentSurface
→ native action
```

## Frozen prohibitions

- No classical fallback inside evidence-bearing CO runtime.
- No adapter policy conclusions, oracle values, shortest-path routes, DP/Q values, or best-action hints.
- No action-name bonuses for resolver recognition.
- No treating weak decision-slot competition as strong rivalry.
- No broad reward tuning before formula/trace gates are passed.

## Current structurally active mechanisms

- public-effect publication by adapters;
- kernel-side relation derivation;
- branch-internal carrier/resolver operations;
- RCF field propagation;
- CollapseCertificate gating;
- certificate-aware stable continuation;
- resolver-aware reopen/sample correction;
- burden-adequate resolver thresholding.

## Frozen behavior-affecting readout constants

The current active readout constants are frozen for structural and empirical sanity testing. Their values are documented in `FORMULA_COEFFICIENT_LEDGER_2026-05-16.md`. They are **provisional structural constants**, not tuned performance parameters.

## Known open watchpoints

- `architecture_acceptance_audit_v1` still reports `ACCEPTANCE_WATCHPOINTS_REMAIN`.
- Formula grounding is improved but not complete.
- Quotient/equivalence tolerance remains target-state rather than finished formalism.
- Recursion scheduler/budget remains target-state rather than finished formalism.
- Multi-step continuation identity remains incomplete.
- Empirical usefulness is not proven.
- RCF novelty vs known algorithms is not proven.

## Stage transition rule

Move from this baseline to empirical sanity only after:

1. structural microcases pass without watchpoints;
2. real-adapter structural trace reviews have no unresolved structural watchpoints;
3. mechanism ablations show interpretable CO-causal effects;
4. behavior-affecting constants are at least ledgered and marked provisional/constrained;
5. no active adapter/fallback path is doing hidden classical rescue.

## Supersession note — 2026-05-17

The 2026-05-16 frozen baseline was intentionally broken for one structural doctrine correction: shape-gauged pre-blocking resolver timing.  The correction is documented in `SHAPE_GAUGED_RESOLVER_TIMING_UPDATE_2026-05-17.md` and constrained by `shape_gauged_resolver_timing_probe_v1` plus the updated `mid_regime_repair_timing_probe_v1`.

This means empirical reports generated before the 2026-05-17 update are historical diagnostics for the prior baseline.  A new freeze document should be produced only after the shape-gauged timing constants are accepted for the next empirical baseline.
