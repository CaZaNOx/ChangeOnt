# EF Router

Status: investigatory / inactive for certified runtime.

The certified CO kernel does not use an in-kernel route selector between CO and non-CO behavior. Any future router-like mechanism must be reintroduced through docs-first review and must not act as a rescue path.
