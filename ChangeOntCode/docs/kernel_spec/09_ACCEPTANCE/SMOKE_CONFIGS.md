# Smoke Configs

## QA Smoke Configs (fast)
- `outputs/verify/configs/qa_bandit.yaml`
- `outputs/verify/configs/qa_maze.yaml`

## QA-Only Agent Variant
- `CO_full_QA` lives in `experiments/configs/co_agents/co_agents_selection.yaml` and is for QA evidence only.
- `CO_full` remains the real/default CO variant.

